# Understanding Open API Specifications

OpenAPI contracts describe every REST capability your agents can call. Learn how to define concise schemas, annotate error shapes, and bundle examples so the tooling can generate helpers automatically.

We highlight validation tooling and how to keep specs synchronized with live services inside the monorepo.
